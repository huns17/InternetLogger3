/** 
 * Copyright © INOVUA TRADING.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { TypeDataSource } from '../../types';
declare type DataPromiseResult = {
    data: any[];
    count: number;
};
declare type DataPromise = Promise<DataPromiseResult>;
declare const useLoadDataEffect: ({ getDataSource, }: {
    getDataSource: ({ shouldReload, }: {
        shouldReload: boolean;
    }) => TypeDataSource;
}, fn: (dataSource: TypeDataSource, { shouldReload, intercept, }: {
    shouldReload: boolean;
    intercept: (promise: DataPromise, dataToLoad: TypeDataSource) => DataPromise;
}) => Promise<any>, { reloadDeps, noReloadDeps }: {
    reloadDeps: any[];
    noReloadDeps: any[];
}) => Promise<any>;
export default useLoadDataEffect;
