/** 
 * Copyright © INOVUA TRADING.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var sameColumns = function sameColumns(cols1, cols2) {
  return cols1.reduce(function (acc, col1, index) {
    var col2 = cols2[index];
    return acc && col1.id === col2.id && col1.name === col2.name;
  }, true);
};

var _default = function _default(newProps, currentProps, state) {
  var newColumnOrder = newProps.columnOrder == null ? state.columnOrder : newProps.columnOrder;

  if (newProps.columns && (newProps.columns.length !== currentProps.columns.length || !sameColumns(newProps.columns, currentProps.columns)) && newProps.columnOrder == null) {
    newColumnOrder = null;
  }

  if (!Array.isArray(newColumnOrder)) {
    newColumnOrder = null;
  }

  return newColumnOrder;
};

exports.default = _default;