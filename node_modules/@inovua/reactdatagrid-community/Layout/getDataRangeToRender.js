/** 
 * Copyright © INOVUA TRADING.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var getDataRangeToRender = function getDataRangeToRender(bodyHeight, itemHeight, scrollTop) {
  var extraRows = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 4;
  extraRows += 2;
  var noItemsToRender = parseInt(bodyHeight / itemHeight + extraRows, 10);

  if (noItemsToRender % 2 === 0) {
    noItemsToRender += 1;
  }

  if (scrollTop <= extraRows / 2 * itemHeight) {
    return {
      from: 0,
      to: noItemsToRender
    };
  }

  var renderFrom = parseInt(scrollTop / itemHeight - extraRows / 2, 10);

  if (renderFrom % 2 !== 0) {
    renderFrom += 1;
  }

  var rangeToRender = {
    from: renderFrom,
    to: renderFrom + noItemsToRender
  };
  return rangeToRender;
};

var _default = getDataRangeToRender;
exports.default = _default;