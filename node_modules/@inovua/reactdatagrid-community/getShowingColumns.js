/** 
 * Copyright © INOVUA TRADING.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _default = function _default(columns, oldColumnsMap) {
  var showingColumnsMap = {};

  if (Object.keys(oldColumnsMap).length) {
    columns.forEach(function (c) {
      var id = c.id || c.name;

      if (c.visibilityTransitionDuration || c.showTransitionDuration) {
        if (!oldColumnsMap[id] || oldColumnsMap[id].visible === false && c.visible !== false && !oldColumnsMap[id].inTransition) {
          if (c.id) {
            showingColumnsMap[c.id] = c;
          }

          if (c.name) {
            showingColumnsMap[c.name] = c;
          }
        }
      }
    });
  }

  return showingColumnsMap;
};

exports.default = _default;