/** 
 * Copyright © INOVUA TRADING.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = diff;

function diff(original, modified) {
  var added = Object.keys(modified).filter(function (k) {
    return !(k in original);
  });
  var deleted = Object.keys(original).filter(function (k) {
    return !(k in modified);
  });
  var updated = Object.keys(modified).filter(function (k) {
    return k in original;
  }).filter(function (k) {
    return JSON.stringify(original[k]) !== JSON.stringify(modified[k]);
  });
  return {
    deleted: deleted,
    updated: updated,
    added: added
  };
}